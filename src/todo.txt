need to make sure todos with no extra labels don't have a link

goals for Tuesday demo:
  - get better story for app
  - list existing tasks
  - add feature to add tasks 
  - basic backend

clean up CSS + text size split, make sure everything is in the style struct
clean up header + menu